# =============================================================
# PSG Analytics API — dépendances du container Docker
# =============================================================
# Fichier allégé : uniquement ce que l'API FastAPI nécessite.
# Ne pas ajouter Streamlit, dbt, pandas ou jupyter ici.
# =============================================================

# --- Serveur FastAPI -----------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- BigQuery ------------------------------------------------
google-cloud-bigquery>=3.25.0
db-dtypes>=1.2.0

# --- Utilitaires ---------------------------------------------
python-dotenv>=1.0.0
