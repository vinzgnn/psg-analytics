# =============================================================
# PSG Analytics — dépendances Python
# =============================================================

# --- Phase 1 — Setup & test API ------------------------------
requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.2.0
jupyter>=1.0.0

# --- Phase 2 — BigQuery & dbt --------------------------------
google-cloud-bigquery>=3.25.0
pandas-gbq>=0.23.0
db-dtypes>=1.2.0
dbt-bigquery>=1.11.0

# --- Phase 4 — à venir ---------------------------------------
# - streamlit, anthropic (à ajouter en Phase 4)
